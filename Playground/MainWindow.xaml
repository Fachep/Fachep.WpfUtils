<Window x:Class="Playground.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Playground"
        xmlns:wpfUtils="clr-namespace:Fachep.WpfUtils;assembly=Fachep.WpfUtils.ViewManager"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:MainWindowViewModel}"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
    </Window.Resources>

    <StackPanel>
        <ComboBox SelectedValue="{Binding CurrentPage}">
            <ComboBoxItem Content="Page1" />
            <ComboBoxItem Content="Page2" />
        </ComboBox>
        <Frame Content="{wpfUtils:GetView CurrentPage}" Height="400" NavigationUIVisibility="Hidden" />
    </StackPanel>
</Window>
